#!/bin/bash

# Script para Vercel - Siempre permitir build para mantener deploy automático.
# Uso: En Vercel Settings → Git → Ignored Build Step → bash ignore-build-step.sh

echo "✅ Deploy automático habilitado: ejecutando build en cada push"
exit 0
